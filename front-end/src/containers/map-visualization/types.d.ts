export interface ViewPortTypes {
  zoom?: number;
  pitch?: number;
  longitude?: number;
  latitude: number;
}

export interface TooltipData {
  // dataset: string;
  // indicator: string;
  title: string;
  value: number;
  // year: number;
  unit: string;
}
